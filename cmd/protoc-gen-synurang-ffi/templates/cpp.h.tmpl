// Code generated by protoc-gen-synurang-ffi. DO NOT EDIT.

#ifndef {{.CppGuardName}}
#define {{.CppGuardName}}

#include <string>
#include <functional>
#include <memory>

#include "{{.PbHeaderFile}}"
{{- range .CppDepHeaders}}
#include "{{.}}"
{{- end}}
{{if .CppNamespace}}
namespace {{.CppNamespace}} {
{{end}}
{{range $svc := .Services}}
// =============================================================================
// {{$svc.GoName}} FFI Server Interface
// =============================================================================

class {{$svc.GoName}}FfiServer {
public:
  virtual ~{{$svc.GoName}}FfiServer() = default;
{{- range $m := .Methods}}
  virtual {{$m.OutputCppType}} {{$m.GoName}}(const {{$m.InputCppType}}& request) = 0;
{{- end}}
};

// =============================================================================
// {{$svc.GoName}} FFI Dispatcher
// =============================================================================

inline std::string {{$svc.GoName}}Invoke({{$svc.GoName}}FfiServer* server, const std::string& method, const std::string& data) {
{{- range $m := .Methods}}
  if (method == "{{$m.FullMethodName}}") {
    {{$m.InputCppType}} req;
    if (!req.ParseFromString(data)) {
      return "";  // Parse error
    }
    {{$m.OutputCppType}} resp = server->{{$m.GoName}}(req);
    return resp.SerializeAsString();
  }
{{- end}}
  return "";  // Unknown method
}

// =============================================================================
// {{$svc.GoName}} FFI Client Connection - routes calls through FFI
// =============================================================================

class {{$svc.GoName}}FfiChannel {
public:
  explicit {{$svc.GoName}}FfiChannel({{$svc.GoName}}FfiServer* server) : server_(server) {}

  template<typename Request, typename Response>
  bool Invoke(const std::string& method, const Request& request, Response* response) {
    std::string data = request.SerializeAsString();
{{- if $.CppNamespace}}
    std::string result = ::{{$.CppNamespace}}::{{$svc.GoName}}Invoke(server_, method, data);
{{- else}}
    std::string result = {{$svc.GoName}}Invoke(server_, method, data);
{{- end}}
    if (result.empty()) {
      return false;
    }
    return response->ParseFromString(result);
  }

private:
  {{$svc.GoName}}FfiServer* server_;
};

// =============================================================================
// {{$svc.GoName}} FFI Client
// =============================================================================

class {{$svc.GoName}}FfiClient {
public:
  explicit {{$svc.GoName}}FfiClient({{$svc.GoName}}FfiChannel* conn) : conn_(conn) {}
  explicit {{$svc.GoName}}FfiClient({{$svc.GoName}}FfiServer* server)
      : owned_conn_(std::make_unique<{{$svc.GoName}}FfiChannel>(server)), conn_(owned_conn_.get()) {}
{{range $m := .Methods}}
  bool {{$m.GoName}}(const {{$m.InputCppType}}& request, {{$m.OutputCppType}}* response) {
    return conn_->Invoke("{{$m.FullMethodName}}", request, response);
  }
{{end}}
private:
  std::unique_ptr<{{$svc.GoName}}FfiChannel> owned_conn_;
  {{$svc.GoName}}FfiChannel* conn_;
};

{{end}}
{{if .CppNamespace}}
} // namespace {{.CppNamespace}}
{{end}}
#endif // {{.CppGuardName}}
