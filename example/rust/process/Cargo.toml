[package]
name = "process_child"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.11"
tonic-health = "0.11"
prost = "0.12"
tokio = { version = "1.0", features = ["full"] }
tokio-stream = { version = "0.1", features = ["net"] }
synurang_service = { path = "../service" }
